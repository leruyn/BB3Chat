package com.bb3.bb3chat.core.platform

/** Controls status bar icon contrast against the screen background. */
enum class AppStatusBarStyle {
    /** Dark background — white status bar icons. */
    DarkBackground,
    /** Light background — dark status bar icons. */
    LightBackground,
}
